#!/usr/bin/python3
""" This module define the function to_str(). """


def to_str(n: float) -> str:
    """ Returns the string representation of the float.

    Args:
        n (float): Float number to be represented.

    Returns:
        str: string representation of 'n'.
    """
    return str(n)
